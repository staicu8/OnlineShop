@model Category
@{ ViewData["Title"] = "Editează Categorie"; }

<div class="mb-4">
    <h1>✏️ Editează Categoria: @Model.Name</h1>
</div>

<div class="row">
    <div class="col-md-6">
        <form asp-action="EditCategory" method="post">
            <input type="hidden" asp-for="Id" />

            <div class="mb-3">
                <label asp-for="Name" class="form-label">Nume Categorie *</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="mb-3">
                <label asp-for="Description" class="form-label">Descriere (opțional)</label>
                <textarea asp-for="Description" class="form-control" rows="3"></textarea>
                <span asp-validation-for="Description" class="text-danger"></span>
            </div>

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-warning">💾 Salvează Modificările</button>
                <a href="@Url.Action("Categories", "Admin")" class="btn btn-secondary">Anulează</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
