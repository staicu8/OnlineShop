@model IEnumerable<Category>
@{ ViewData["Title"] = "Gestiune Categorii"; }

<div class="mb-4 d-flex justify-content-between align-items-center">
    <h1>📂 Gestiune Categorii</h1>
    <div>
        <a href="@Url.Action("CreateCategory", "Admin")" class="btn btn-success">➕ Adaugă Categorie</a>
        <a href="@Url.Action("Dashboard", "Admin")" class="btn btn-secondary">← Dashboard</a>
    </div>
</div>

<div class="table-responsive">
    <table class="table table-hover">
        <thead class="table-light">
            <tr>
                <th>ID</th>
                <th>Nume</th>
                <th>Descriere</th>
                <th>Nr. Produse</th>
                <th>Acțiuni</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var category in Model)
            {
                <tr>
                    <td>#@category.Id</td>
                    <td><strong>@category.Name</strong></td>
                    <td>@(category.Description ?? "-")</td>
                    <td>
                        <span class="badge bg-info">@(category.Products?.Count ?? 0) produse</span>
                    </td>
                    <td>
                        <a href="@Url.Action("EditCategory", "Admin", new { id = category.Id })" class="btn btn-sm btn-warning">
                            ✏️ Editează
                        </a>
                        <form asp-action="DeleteCategory" asp-route-id="@category.Id" method="post" style="display:inline">
                            <button type="submit" class="btn btn-sm btn-danger" 
                                    onclick="return confirm('ATENȚIE! Ștergerea categoriei va șterge și toate produsele (@(category.Products?.Count ?? 0)) din această categorie. Continuați?')">
                                🗑️ Șterge
                            </button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">Nu există categorii. Adaugă una!</div>
}
